//======================================================
// interrupt routines
//======================================================
#include "h\Allinclude.h"

/*----------------------------------------------------------------------------------------------------------*/
/*				定时器3中断处理函数																			*/
/*----------------------------------------------------------------------------------------------------------*/


/*----------------------------------------------------------------------------------------------------------*/
/*				定时器3中断处理函数																			*/
/*----------------------------------------------------------------------------------------------------------*/
void Timer3_Interrupt(void)		interrupt		5
{	
	//-----------------------------------------------------------
	// 压栈保护
    _push_(INSCON);
	INSCON = 0;
    _push_(FLASHCON);
    FLASHCON = 0;
	//-----------------------------------------------------------
	// 清中断标志
    SET_BANK_1();
    TF3 = 0;
    SET_BANK_0();
	//-----------------------------------------------------------
	// 中断处理内容
    F_TimeBaseTick_IRQ();
	F_BellIrq();
	//-----------------------------------------------------------
	// 出栈
    INSCON = 0;
	_pop_(FLASHCON);
    _pop_(INSCON);
}